# 滚动流程数据
本工作流是**滚动流程数据**的示例。

## 背景
对模型进行滚动训练时，需要为各个滚动窗口生成对应数据。滚动窗口发生滑动后，训练数据随之改变，数据处理器的可学习状态（例如标准差、均值等）也会发生变化。

为避免重复生成数据，该示例采用基于`DataHandler`的数据加载器，加载与滚动窗口无关的原始特征，再通过处理器生成依赖滚动窗口的处理后特征。

## 运行代码
执行如下命令运行示例：
```bash
python workflow.py rolling_process
```